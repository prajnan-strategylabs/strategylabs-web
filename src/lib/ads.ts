/**
 * AdMob advertising has been completely removed to preserve the premium
 * trading terminal aesthetic of Strategy Labs.
 * 
 * Empty stub functions are maintained here to prevent import failures.
 */

export async function initializeAdMob(): Promise<void> {
  // AdMob completely removed to ensure premium UX
}

export async function showBanner(): Promise<void> {
  // AdMob completely removed to ensure premium UX
}

export async function hideBanner(): Promise<void> {
  // AdMob completely removed to ensure premium UX
}

export async function showInterstitial(): Promise<boolean> {
  // AdMob completely removed to ensure premium UX. 
  // Always return true to let all premium trading flows proceed immediately.
  return true;
}
